# ─── CLIENT ENVIRONMENT (EXAMPLE) ────────────────────────────────────────────
# Copy this file to .env and fill in your values. Never commit .env directly.
# React env vars MUST be prefixed with REACT_APP_

# Backend API / Socket.IO URL
# → Local:      http://localhost:5000
# → Production: https://online-compiler-backend-36dz.onrender.com
REACT_APP_API_URL=http://localhost:5000
